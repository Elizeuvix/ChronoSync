using UnityEngine;

namespace CS.Base
{
    // Simple placeholder to drive remote animator parameters from movement if needed.
    // Currently no behavior; acts as a marker component to avoid compile errors.
    public class RemoteAnimatorDriver : MonoBehaviour
    {
    }
}
